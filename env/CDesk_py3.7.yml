name: CDesk_py3.7
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.7
  - pip>=21.3
  - setuptools>=57.0,<65.0
  - wheel
  - hicexplorer
  - pairtools
  - bwa
  - cython
  - pip
  - pip:
    # 使用完整 pip install 命令，指定官方源或国内镜像
    - -r pip_requirements.txt
